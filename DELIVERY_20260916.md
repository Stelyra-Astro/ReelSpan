# Reel Atlas · 2026-09-16 交付说明

本交付基于用户上传的 `ReelSpan-main(1).zip` 修改，未从 GitHub 获取或推送代码。

## 已实现

- App 取消 `story_movie_target_matches` 对电影总目录的 551 部限制：按 `story_movies.legacy_id` 每批 250 部同步，同时写入关联的故事地点、时间与旧目标匹配。首批完成后即可使用；其余批次异步同步，批次事务提交同步游标，重启后续传。数据指纹与本地格式一致且全部完成时跳过重新下载。
- Supabase 全库 `reelatlas_discover` 检索电影名、元数据、故事地点、时期和已核实概念标签；相关电影优先，同期电影随后。统一搜索由服务端执行，不受手机离线下载进度影响。
- When 分类：年代／人物（生存年代）／事件／政权／历史时代；只有完整起止年份的概念可作为精确时间筛选。Where 仅列出现代国家、行政区和城市；两者独立。添加了列表／地图切换，列表布局按 HTML 原型适配为原生 SwiftUI。
- 修复了世纪／年代被记录为单一年份的问题；原始数据修改前的 10 张表数据快照在 `reelspan_backup_20260916` schema 中。同步发布版本 `2026-09-16-full-catalog-when-search-v1`。

## 注意事项

- 快照在同一个 Supabase 项目内，**不是异地备份，也不包含 Storage 文件或完整可重建的数据库角色／权限**。请另外用 Supabase CLI/pg_dump 导出离线备份，不要删除备份 schema。
- When 目录现有 714 个概念，但人物等标签并非全覆盖。只有证据支持的 4 部影片被直接标记为满洲国；其余仅年代重叠的影片不能标成满洲国主题。大量影片仍缺少明确故事时间或 TMDB 元数据，不能凭空补齐。
- iOS 不保证进程进入后台后持续联网；暂停时保留游标，下次打开继续。首次同步预计首批 250 部，再逐批增加。
- 未修改原先的冷启动地图定位逻辑。`ReelAtlas-iOS/supabase/migrations/202609160001` 至 `000007` 是此次变更的 SQL 参考副本，**已在当前生产 Supabase 项目执行，请勿对该项目再次直接 `db push` 重放**（云端迁移版本号与本地文件名不同）。
- 本环境为 Linux，没有 Apple SDK/Xcode，无法完成 iOS 真正的类型检查或模拟器构建。Mac 上请执行：`xcodebuild -project ReelAtlas-iOS/ReelSpan.xcodeproj -scheme ReelSpan -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build`，然后真机验证首批显示、重启续传、断网与搜索结果。

## 验证

源码中的 Python 项目契约测试、Swift 语法解析及 SQLite schema 初始化均在打包前检查；实际 iOS 编译和真机运行仍需 Mac 验收。
